# 生产环境变量
# 在 Vercel Dashboard 的 Environment Variables 中设置

# Vercel Postgres 连接字符串（从 Vercel Postgres 获取）
DATABASE_URL="postgresql://..."

# NextAuth（必填，生成随机字符串：openssl rand -base64 32）
AUTH_SECRET="your-production-auth-secret-here"

# 应用地址
NEXTAUTH_URL="https://your-domain.vercel.app"
